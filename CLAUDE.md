# Project notes

- Whenever a user-facing feature is added or changed, update the in-app
  Help screen (`src/components/HelpModal.tsx`) to reflect it, as part of
  the same change — don't wait to be asked separately.
